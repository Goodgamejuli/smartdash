import React from 'react';

/**
 * TopologyCanvas will use React Flow to render nodes and edges representing
 * smart-home devices and their connections. For now it shows a placeholder.
 */
const TopologyCanvas: React.FC = () => {
  return (
    <main className="bg-white rounded-xl border p-2">
      <p>Zeichenfläche für Geräte und Verbindungen (React Flow)</p>
    </main>
  );
};

export default TopologyCanvas;
